from setuptools import setup

setup(name = "pykvfs",
      version = "0.1",
      description = "pykvfs is a filesystem-backed transactional key-value",
      author = "Gilles Chehade",
      author_email = "gilles@poolp.org",
      packages = [ "pykvfs", ],
)
